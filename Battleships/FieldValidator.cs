namespace Battleships
{
    public static class FieldValidator
    {
        public static bool ValidateField(int[,] field)
        {
            //TODO: Implement either version of the rules, with or without adjacent ships. Pick the first test class for the rules without adjacency. 
            return false;
        }
    }
}